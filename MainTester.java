public class MainTester{
	public static void main(String[] args){ 
		if(Character.isDigit(CompressedText.charAt(0))){
			return CompressText.charAt(0) + decompress(CompressedText.substring(0,CompressedText.length));
		} else {
			return + CompressedText.substring(
	}
}